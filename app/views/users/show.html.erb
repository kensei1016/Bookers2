<div class="container">
  <p id="notice"><%= notice %></p>
  <div class="row">
    <div class="col-3">
      <%= render 'users/profile', {user: @user, book: @book} %>
    </div>
    <div class="col-9">
      <h2>Books</h2>
      <%= render 'books/books_table', books: @books %>
    </div>
  </div>
</div>