<div class="container">
  <p id="notice"><%= notice %></p>
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= @book.errors.count %>errors prohibited this obj from being saved:</h2>
      <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row">
    <%# userprofile %>
    <div class="col-xs-3">
      <%= render 'users/profile', {user: @user, book: @book} %>
    </div>
    <%# userprofile/ %>
    <%# Books %>
    <div class="col-xs-9">
      <h2>Books</h2>
      <div class="search-form-wrapper">
        <%= search_form_for @q, class: "form-inline" do |f| %>
          <div class="form-group">
            <%= f.label :title_cont, 'Title', class: 'sr-only' %>
            <%= f.search_field :title_cont, class: 'form-control', placeholder: 'Title' %>
          </div>
          <div class="form-group">
            <%= f.label :body_cont, 'Opinion', class: 'sr-only' %>
            <%= f.search_field :body_cont, class: 'form-control', placeholder: 'Opinion' %>
          </div>
          <%= f.submit class: "btn btn-primary" %>
        <% end %>
      </div>
      <%= render 'books/books_table', books: @books %>
    </div>
    <%# Books/ %>
  </div>
</div>